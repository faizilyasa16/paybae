{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
        "builder": "NIXPACKS"
    },
    "deploy": {
        "startCommand": "uvicorn sistem.deep_recommend_api:app --host 0.0.0.0 --port $PORT"
    }
}
